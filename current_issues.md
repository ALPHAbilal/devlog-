- the math equation block type need to be deleted 
- all block types should their data to be load in the database succefuly  
- make he other block types extadable in case the content is very tall and we want to make sure that user can't scroll all way down just to go to the next block
-
-
- the AI block dosn't have the cllabasble fature where in the scenario of having very long contetn and by default we can't scroll all the way down just to continue to the next block the snippet code block have this feature look into it 

- optimize our application interm of the performance and as well as the decrease how many claas we maid to the supabase (exacly where we  renter already entred document that shouldn't ask the database for the data again) 
- why we makea calls of saving already saved data, 